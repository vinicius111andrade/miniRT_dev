Vector operations
have to define vector space
vector def.: one point + escalar, (x, y, z, escalar)

makeVector(point1, point2)
escaleVector(vector, escalar), returns escalar*Vector
isUnitary(vector), returns 1 or 0
makeUnitary(vector), return vector

sumVector
subtractVector
escaleVector(vector, escalar), returns escalar*Vector

Matrix operantions

sumMatrix
subtractMatrix
multiplyMatrix
transposeMatrix
